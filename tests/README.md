# PredictX Tests

This directory is reserved for integration and end-to-end tests that cross contract and frontend boundaries.

Current unit test placeholders live in `contracts/src/tests.rs`. Business logic tests should be added when market, betting, resolution, and reward behavior is implemented.

